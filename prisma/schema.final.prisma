// FINAL STATE - After backfill and making blobUrl required again
// This is what schema.prisma should look like after Step 3

model Photo {
  id          String   @id @default(uuid())
  originalName String
  mimeType    String
  size        Int      // size in bytes
  storagePath String?  // legacy filesystem path (optional for backward compatibility)
  blobUrl     String   // REQUIRED - canonical field for photo URLs
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([createdAt])
  @@map("photos")
}
